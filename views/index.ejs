<!DOCTYPE html>
<html>

<head>
  <title>Secrets API Interface</title>
  <style>
    /* Traditional CSS Styles */
    body {
      font-family: Arial, sans-serif;
      background-color: #f5f5f5;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 800px;
      margin: 40px auto;
      background-color: #fff;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    h1 {
      text-align: center;
      margin-bottom: 20px;
      color: #2c3e50;
    }

    .description {
      font-size: 14px;
      color: #555;
      margin-bottom: 20px;
      background-color: #eef2f3;
      padding: 10px;
      border-left: 4px solid #3498db;
    }

    form {
      margin-top: 20px;
    }

    label {
      display: block;
      margin-top: 15px;
      margin-bottom: 5px;
      font-weight: bold;
    }

    input[type="text"],
    input[type="number"] {
      width: 100%;
      padding: 8px;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
    }

    input[type="submit"] {
      margin-top: 15px;
      margin-right: 10px;
      color: #fff;
      padding: 10px 16px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 15px;
    }

    .response-area {
      margin-top: 30px;
      background-color: #f0f0f0;
      padding: 15px;
      border-radius: 6px;
      font-family: monospace;
      font-size: 14px;
      white-space: pre-wrap;
      word-wrap: break-word;
    }

    #get {
      background-color: #2ecc71;
    }

    #get:hover {
      background-color: #27ae60;
    }

    #post {
      background-color: #3498db;
    }

    #post:hover {
      background-color: #2980b9;
    }

    #put {
      background-color: #9b59b6;
    }

    #put:hover {
      background-color: #8e44ad;
    }

    #patch {
      background-color: #f1c40f;
      color: #000;
    }

    #patch:hover {
      background-color: #f39c12;
    }

    #delete {
      background-color: #e74c3c;
    }

    #delete:hover {
      background-color: #c0392b;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>Secrets API Project</h1>

    <div class="description">
      <p><strong>GET:</strong> Provide <code>id</code> to retrieve a secret.</p>
      <p><strong>POST:</strong> Provide <code>secret</code> and <code>score</code> to create a new secret.</p>
      <p><strong>PUT:</strong> Provide <code>id</code>, <code>secret</code>, and <code>score</code> to update the entire secret.</p>
      <p><strong>PATCH:</strong> Provide <code>id</code> and one field (like <code>score</code>) to update partially.</p>
      <p><strong>DELETE:</strong> Provide <code>id</code> to remove the secret.</p>
    </div>
    <div class="response-area">
      <%= content %>
    </div>

    <form id="myForm" method="post">
      <label for="idInput">Id:</label>
      <input type="text" id="idInput" name="id">

      <label for="secretInput">Secret:</label>
      <input type="text" id="secretInput" name="secret">

      <label for="scoreInput">Score:</label>
      <input type="number" id="scoreInput" name="score">

      <label>Choose Route:</label>
      <input id="get" type="submit" value="GET" formaction="/get-secret">
      <input id="post" type="submit" value="POST" formaction="/post-secret">
      <input id="put" type="submit" value="PUT" formaction="/put-secret">
      <input id="patch" type="submit" value="PATCH" formaction="/patch-secret">
      <input id="delete" type="submit" value="DELETE" formaction="/delete-secret">
    </form>

    
  </div>
</body>

</html>
